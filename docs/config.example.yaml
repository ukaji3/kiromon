# kiromon 設定ファイルサンプル
# 配置場所: ~/.config/kiromon/config.yaml
#
# このファイルはオプションです。存在しない場合は組み込みのデフォルト値が使用されます。
# コマンドラインオプションは設定ファイルより優先されます。

# デフォルトの通知コマンド
# -c オプションを省略した場合に使用されます
default_command: notify-send

# ログファイルパス
# -log オプションを省略した場合に使用されます
log_path: ~/kiromon.log

# コマンドごとのプリセット設定
# コマンド名をキーとして、通知コマンド、プロンプトパターン、メッセージを設定できます
# プロンプトパターンはコマンドごとに異なるため、プリセットで個別に設定してください
presets:
  # kiro-cli 用のプリセット
  kiro-cli:
    command: voicevox-speak-standalone
    prompt_pattern: '> ?$'
    start_msg: "{time}、タスクを開始したのだ"
    end_msg: "{time}、タスクを終了したのだ。処理時間は、{duration}だったのだ。"

  # 汎用的なプリセット例
  # vim:
  #   command: notify-send
  #   end_msg: "vim を終了しました"
  #
  # python:
  #   command: say
  #   prompt_pattern: '>>> $'
  #   start_msg: "Python スクリプト実行中..."
  #   end_msg: "Python スクリプト完了"
  #
  # bash:
  #   prompt_pattern: '\$ $'
